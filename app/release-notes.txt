Bug fixes and a new Bump action.

- Fixed: app crashing repeatedly on newer Android versions (Android 14+).
- Fixed: editing a scheduled task's time now updates immediately in the list.
- Fixed: scheduled task no longer shows up twice in the list.
- New: "Bump" button in the notification — sends the current task to the
  bottom of the list permanently (different from Snooze, which is temporary).
