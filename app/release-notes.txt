Issue #6 - Snoozed tasks now reenter the queue automatically.

When a task is snoozed, the app now sets an alarm for when the snooze
expires. When the alarm fires, the task re-enters the notification
without any manual intervention (no need to open the app or mark it
complete/uncomplete to trigger a refresh).
