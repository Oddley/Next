Bug fixes for issues #4 and #5.

- Fix #5: notification could show "All caught up" even when tasks exist.
  Emission alarm now restarts the notification service if the OS killed it
  while no tasks were active.

- Fix #4: scheduled task times (Today/Tomorrow) now refresh every minute
  in the list, so the label updates correctly after midnight instead of
  staying stale until the next emission fires.
